#include "main.h"
#include <stdlib.h>

/**
 * _abs - prints the absolute of a number
 * @n: is an int
 * Description: _abs uses the _abs prototype
 * Return: it returns asbolute value of parameter n
 */

int _abs(int n)
{
	/* return the absolute result */
	return (abs(n));
}
