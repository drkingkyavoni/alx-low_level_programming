#include "main.h"
#include <string.h>

/**
 * _strlen - returns length of a string
 * Description: uses printf function
 * @s: an char pointer parameter
 * Return: int
 */

int _strlen(char *s)
{
	return (strlen(s));
}
